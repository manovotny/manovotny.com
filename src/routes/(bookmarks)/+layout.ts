// The site root sets `csr = false; prerender = true`. Bookmarks is a real
// app: server-rendered per request, hydrated on the client.
export const csr = true;
export const prerender = false;
export const ssr = true;
