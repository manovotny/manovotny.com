default:
  - build

init:
  - config
  - shell:install
  - update
  - build

config:
  - replace

build:
  - lint
  - test

generate:
  - makepot

update:
  - clean:lib
  - shell:update
  - copy
  - build

lint:
  - phplint

test:
  - phpunit

release:
  - build
  - replace:project_version
  - generate
