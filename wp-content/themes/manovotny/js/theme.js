jQuery( 
	function( $ )
	{
		var $offset = -30;
		
		$( '#social-email' ).tipsy( { offset: $offset } );
		$( '#social-twitter' ).tipsy( { offset: $offset } );
		$( '#social-forrst' ).tipsy( { offset: $offset } );
		$( '#social-github' ).tipsy( { offset: $offset } );
		$( '#social-wordpress' ).tipsy( { offset: $offset } );
	}
);